extends base
block content 
    main 
        .login_require
            h1 Bạn chưa đăng nhập, vui lòng đăng nhập!
            //- a.login_btn(href='http://localhost:4000/auth/login') Đăng nhập
            //- a.login_btn(href='http://localhost:4000/auth/signup') Đăng ký
            .form__group
                button.btn.btn--green(type='button', onclick="login()") Đăng nhập
                button.btn.btn--green(type='button', onclick="signup()") Đăng ký
    script. 
        function login(){
            console.log(11111)
            window.location.href='http://localhost:4000/auth/login'
        }