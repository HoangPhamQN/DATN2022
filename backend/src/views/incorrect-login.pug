extends base
block content 
    main 
        .login_require
            h1 Mật khẩu đăng nhập hoặc email chưa đúng, vui lòng thử lại!
            .form__group
                button.btn.btn--green(type='button', onclick="login()") Thử lại
    script. 
        function login(){
            window.location.href='http://localhost:4000/auth/login'
        }