header.header
    nav.nav.nav--tours
        a.nav__el(href='/merchaindise/tat-ca-mat-hang') Tất cả mặt hàng
        //- a.nav__el(href='' data-toggle='dropdown') Vật tư y tế
        //- a.nav__el(href='/merchaindise/thuoc') Thuốc
        .dropdown
            a.nav__el.dropdown-toggle(href='/merchaindise/thuoc', role='button', data-bs-toggle='dropdown', aria-expanded='false')
                | Thuốc
            ul.dropdown-menu
                each item in medicals
                    li
                    a.dropdown-item(href='#')= item
        .dropdown
            a.nav__el.dropdown-toggle(href='/merchaindise/vat-tu-y-te', role='button', data-bs-toggle='dropdown', aria-expanded='false')
                | Vật tư y tế
            ul.dropdown-menu
                each item in supplies
                    li
                    a.dropdown-item(href='#')= item
        //- form.nav__search
        //- button.nav__search-btn
        //-     svg
        //-     use(xlink:href='img/icons.svg#icon-search')
        //- input.nav__search-input(type='text' placeholder='Search tours')
    .header__logo
        img(src='/imgs/logo-white.png' alt='Natours logo')
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout Log Out
            a.nav__el(href='/me')
                img.nav__user-img(src=`/img/users/${user.photo}` alt='User photo')
                span= user.name.split(' ')[0]
        else
            a.nav__el.nav__el-cta(href='#') Đăng kí
            a.nav__el.nav__el-cta(href='/auth/login') Đăng nhập