header.header
    nav.nav.nav--tours
        a.nav__el(href='/merchaindise') Tất cả mặt hàng
        a.nav__el(href='/merchaindise') Vật tư y tế
        a.nav__el(href='/merchaindise') Thuốc
        //- form.nav__search
        //- button.nav__search-btn
        //-     svg
        //-     use(xlink:href='img/icons.svg#icon-search')
        //- input.nav__search-input(type='text' placeholder='Search tours')
    .header__logo
        img(src='/imgs/logo-white.png' alt='Natours logo')
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout Log Out
            a.nav__el(href='/me')
                img.nav__user-img(src=`/img/users/${user.photo}` alt='User photo')
                span= user.name.split(' ')[0]
        else
            a.nav__el.nav__el-cta(href='#') Đăng kí
            a.nav__el.nav__el-cta(href='/login') Đăng nhập